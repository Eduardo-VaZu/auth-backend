#!/usr/bin/env sh

echo "[pre-push] Checking Postman sync state..."
npm run postman:sync:check
status=$?

if [ "$status" -ne 0 ]; then
  echo "[pre-push] Blocked: Postman is not synced. Run: npm run postman:sync"
  exit "$status"
fi

echo "[pre-push] OK"
